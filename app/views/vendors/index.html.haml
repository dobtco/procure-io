- provide :page_action, t('g.vendors')

= render "admin/nav"

#searchable-table-page.search-page
  :javascript
    new ProcureIo.Backbone.SearchableTable({
      collection: ProcureIo.Backbone.VendorList,
      columns: [
        {
          name: "#{Vendor.human_attribute_name(:name)}",
          accessor: 'name',
          sortKey: 'name',
          defaultSort: 'asc'
        },
        {
          name: "#{User.human_attribute_name(:email)}",
          accessor: 'user.email',
          sortKey: 'email'
        }
      ],
      actions: ["edit", "destroy"]
    });
