.hero-unit
  %h1= t('.hero.header')
  %p= t('.hero.content.line_html', link: link_to(t('.hero.content.link'), new_vendor_path))

%h4
  = t('g.featured_projects')
  %small= link_to t('g.see_all'), projects_path

.row-fluid.featured-projects
  - @featured_projects.each do |project|
    .span4.project
      = link_to project.title, project, class: "project-title"
      - if project.featured?
        %i.icon-star
      .abstract
        = project.abstract_or_truncated_body
        = link_to t('g.read_more'), project
      .bids-due
        - if project.bids_due_at
          = t('g.bids_due_on_date', date: project.bids_due_at.to_formatted_s(:readable_dateonly))
        - else
          = t('g.open_for_bids')
