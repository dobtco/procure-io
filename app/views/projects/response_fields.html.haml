- provide :page_action, t('g.response_fields')

= render "projects/header_backend"

#admin-response-field-page
  :javascript
    new ProcureIo.Backbone.AdminResponseFieldPage({
      formName: "#{t('g.bid_form')}",
      bootstrapData: #{bootstrap @project.response_fields.to_json},
      response_fieldable_id: #{@project.id},
      response_fieldable_type: "Project",
      formOptions: #{@project.form_options.to_json}
    });
