- provide :page_title, t('globals.notifications')

!=page_header t('globals.notifications')

%p#no-notifications.hide= t('.none')

#notifications-list
  :javascript
    new ProcureIo.Backbone.NotificationPage({bootstrapData: #{@notifications_json}});

= will_paginate @notifications, class: "pagination pagination-centered"
