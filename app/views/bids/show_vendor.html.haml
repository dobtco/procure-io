- provide :page_action, t('globals.my_bid')

!=page_header @project.title

.row-fluid
  .span7
    %h4= t('globals.project_description')
    %p= simple_format @project.body

  .span5
    %h4= t('globals.my_bid')

    %div
      - if @bid.dismissed?
        %span.badge.badge-important= t('globals.dismissed')
      - elsif @bid.awarded?
        %span.badge.badge-success= t('globals.awarded')
      - else
        %span.badge.badge-info= t('globals.open')

    = link_to t('.return_to_project_html'), @project

    = render "bid"
