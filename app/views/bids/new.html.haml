!=page_header @project.title

.row-fluid
  .span7
    %h4= t('bids.project_description')
    %p= simple_format @project.body

  .span5
    %h4= t('.new_bid')
    - if @project.form_options["form_description"]
      %span.help-block= @project.form_options["form_description"]
    = render "bids/form"