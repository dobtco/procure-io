# == Schema Information
#
# Table name: roles
#
#  id          :integer          not null, primary key
#  name        :string(255)
#  role_type   :integer          default(1), not null
#  permissions :text
#  created_at  :datetime
#  updated_at  :datetime
#  undeletable :boolean
#  default     :boolean
#

admin:
  id: 1
  name: Admin
  role_type: 2
  default: true
